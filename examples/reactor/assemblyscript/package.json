{
  "name": "validator",
  "version": "0.0.1",
  "scripts": {
    "build": "asc validator.ts --exportRuntime --debug -b none -o validator.wasm --transform json-as/transform"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.0"
  },
  "dependencies": {
    "json-as": "^0.9.29",
    "visitor-as": "^0.11.4"
  }
}
